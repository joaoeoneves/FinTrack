*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Firebase client config — not a secret, but keeps project identifiers out of a
# possibly-public remote; each dev/CI pulls it from the Firebase console instead.
google-services.json

# Claude Code subagent activity log — local debug output, not meant to be shared.
.claude/logs/
